{#
/**
 * @file
 * Preload assets.
 *
 * Available variables:
 * - vartheme_claro_path: Returns the path to the Vartheme Claro theme.
 * - html_dir: contains the language direction. It will either be 'ltr' or 'rtl'.
 */
#}

{#
* NOTE: Change the logic in this file to match the need of your website.

* Example of logic: 
    - Multilingual websites may use a language selection for preloaded assets.
    - Sections of the website may have a custom prelead assets for CSS/JS.
*  --------------------------------------------------------------------------
*  Have a look at "Preload critical assets to improve loading speed"
*  https://web.dev/preload-critical-assets/
*  for more ideas on what/when/where to use preload.
#}

{# Preload assets for all languages and all sections in the website. #}

{# Example: Preload assets only for LTR languages.
* By adding more restraction or selection for what/when/where to use for preload.
*  --------------------------------------------------------------------------
{% if html_dir == 'ltr' %}
  <link rel="preload" href="/{{ vartheme_claro_path }}/fonts/lora/lora-v14-latin-regular.woff2" as="font" type="font/woff2" crossorigin="anonymous">
{%- endif -%}
*  --------------------------------------------------------------------------
* Needs to add the font files in the right location for local fonts.
#}

{# Preload assets only for RTL languages. #}
{% if html_dir == 'rtl' %}
{# Preload Tajawal Regular TrueType Font #}
<link rel="preload" href="{{ vartheme_claro_path }}/fonts/rtl/tajawal/webfonts/tajawal-regular-webfont.eot" as="font" type="font/eot" crossorigin="anonymous">
<link rel="preload" href="{{ vartheme_claro_path }}/fonts/rtl/tajawal/webfonts/tajawal-regular-webfont.ttf" as="font" type="font/ttf" crossorigin="anonymous">
<link rel="preload" href="{{ vartheme_claro_path }}/fonts/rtl/tajawal/webfonts/tajawal-regular-webfont.woff" as="font" type="font/woff" crossorigin="anonymous">
<link rel="preload" href="{{ vartheme_claro_path }}/fonts/rtl/tajawal/webfonts/tajawal-regular-webfont.woff2" as="font" type="font/woff2" crossorigin="anonymous">
<link rel="preload" href="{{ vartheme_claro_path }}/fonts/rtl/tajawal/webfonts/tajawal-regular-webfont.svg#tajawalregular" as="font" type="font/svg" crossorigin="anonymous">
{% endif %}

